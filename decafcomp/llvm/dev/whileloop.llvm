; ModuleID = 'C'

declare void @print_int(i32)

define void @f() {
entry:
  %x = alloca i32
  store i32 0, i32* %x
  br label %WhileCond

WhileCond:                                        ; preds = %IfCont, %entry
  %x1 = load i32, i32* %x
  %LTTmp = icmp slt i32 %x1, 10
  %IfCond = icmp eq i1 %LTTmp, true
  br i1 %IfCond, label %While, label %WhileEnd

While:                                            ; preds = %WhileCond
  %x2 = load i32, i32* %x
  %AddTmp = add i32 %x2, 1
  store i32 %AddTmp, i32* %x
  %x3 = load i32, i32* %x
  %EQTmp = icmp eq i32 %x3, 5
  %IfCond4 = icmp eq i1 %EQTmp, true
  br i1 %IfCond4, label %Then, label %Else

Then:                                             ; preds = %While
  br label %WhileEnd
  br label %IfCont

Else:                                             ; preds = %While
  br label %IfCont

IfCont:                                           ; preds = %Else, %Then
  %x5 = load i32, i32* %x
  call void @print_int(i32 %x5)
  br label %WhileCond

WhileEnd:                                         ; preds = %Then, %WhileCond
  ret void
}

define void @main() {
entry:
  call void @f()
  ret void
}
